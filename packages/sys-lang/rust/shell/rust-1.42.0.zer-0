#!/bin/false
# shellcheck shell=bash # This file is used only for sourcing

# Used to source rust.zerlib
requires rust

# Sets metadata for stable/testing channel
# shellcheck disable=SC2034 # Metadata for zernit
PLATFORMS="amd64 ~amd64"